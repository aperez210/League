motd = "#gamer#"
msg = input(f"{motd}: ")
output = ""

import main.py

def getKeywords(inp:str)->list:
    output = inp.split(" ")
while msg != "stop":
    msg = input(f"{motd}: ")
    match msg:
        case :
            def search: